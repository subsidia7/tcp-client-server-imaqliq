# tcp-client-server-imaqliq

# Compile example using g++:
g++ server.cpp -o server

g++ client.cpp -o client

# To run client write:
client [filename]
# If you have file "text.txt" the command will be:
client text.txt
